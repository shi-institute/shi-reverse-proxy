# @shi-institute/utils

Shared utility functions used across Shi Institute web packages. Includes helpers for `Server-Timing` header construction, hashing (via `fast-sha256`), and XSS sanitization (via `xss`).

## Build

```sh
pnpm build
```
