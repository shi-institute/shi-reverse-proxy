---
'@shi-institute/custom-elements': minor
---

The menu element (the button to trigger the side menu and the associated side menu) is now exposed as shi-navigation-bar-menu. Menu items must be set via JavaScript by setting the `items` property on the element. Other props can be set as attributes and are reflected.
